image: ruby:2.3

pages:
  script:
  - gem install redcarpet
  - gem install jekyll
  - jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master

staging:
  script:
  - jekyll build -d public
  - gem install dpl
  - dpl --provider=firebase --token=1/2GJtCAQ8E88zQHgZl5cysXC3YqaIKsVVpOlpkM0U2Lg --project=test-www
  artifacts:
    paths:
    - public
  only:
  - develop