image: ruby:2.1

pages:
  stage: deploy
  environment: production
  script:
  - gem install redcarpet
  - gem install jekyll
  - jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master
